# 提出用の下書き（Zenn ダッシュボードに貼る文面）

締切 2026-10-15 23:59。提出後も締切までは編集できる。デフォルトブランチは提出時点のまま 12/1 まで凍結。デプロイは 12/1 まで維持。

## プロジェクト概要（200〜300 文字。提出期限後に公開。現在 296 字）

プロジェクト名: あとのこと

あとのことは、いなくなった後のデジタルの持ち物を「そのとおりに」する AI エージェントです。受信箱を一度だけ読んで契約やアカウントを星図に描き、本人が資産ごとに「渡す・消す・残す」を塗ったら、以後は何もしなくていい。毎日 1 回、Google アカウントの既読・送信・買い物の通知・Drive・予定の日時だけを見張り、沈黙が床（30/60/14 日）を越えたら確認者 2 人に問い、本人へ最後の通知をして 7 日待ってから、生前の遺志どおりに止め、渡し、報告します。判断は説明つき、執行は決定的なゲートを通り、封印した資格情報は Confidential Space の中でしか開きません。

## 課題と解決（800 文字まで。Markdown 一部可）

**課題**: 人がいなくなった後、サブスクは払われ続け、写真は誰にも渡らず、SNS は残る。Google の「アカウント無効化管理ツール」は Google アカウントの中だけを扱い、第三者サービスの契約や「何を誰に」という遺志までは扱わない。一方で、家族に全アカウントのパスワードを預けるのは危険で、本人が死んだかどうかを機械が決めるのはもっと危険。

**解決**: 本人の死を「確定」しない設計にした。
- **地図**: 受信箱の棚卸し（ADK の 4 段エージェント）で契約・課金元・ログイン経路・依存を掘り出し、星図に描く。生前は「使っていないのに払っている」が暗い星で見える
- **遺志**: 「写真は妻に」の一言を資産ごとの塗り分けに変換する。曖昧なら聞き返す。パスワードを渡す指示は設計上できない
- **見張り**: 本人の Google の動きだけを裏で追う。沈黙が床を越えたら ADK の調査係が読み取り専用ツールで手掛かり（最終日、予定の題名、買い物、足したサービス）を集め、判断係が見立てと待つ日数を返す。Amazon や楽天、PayPay、銀行振込のような API の無いサービスは、受信箱の注文確認・決済通知の差出人と件名の型だけで「本人が今動いた」を拾う（定期課金は数えない、件名は保存しない）。あとのことへのログインは合図にしない。床（30/60/14 日）はモデルが縮められず、延ばす提案と説明だけができる
- **人の承認**: 確認者 2 人の「本人と連絡が取れない」が要る。その後も本人へ最後の通知を送り、7 日の間に動きが一つでも見えれば止まる
- **執行**: 決定的なポリシーゲートを通った操作だけ。止める → 渡す → ハブは最後。封印した資格情報は Confidential Space の enclave でしか開かず、平文は外に出ない

## アーキテクチャ説明（800 文字まで）

Cloud Run（サービス＋ジョブ）、Cloud Scheduler、Vertex AI Gemini（ADK）、Secret Manager、Cloud Storage、Cloud KMS、Confidential Space、Model Armor、Sensitive Data Protection、Cloud Logging。

- **棚卸し**: ADK の SequentialAgent（scout → linker → classifier → verifier）。受信箱の本文は外部入力として扱い、Model Armor を入口に、注入メールは `report_suspicious_mail` で報告するだけで従わない（注入 10 変種 × N=2 で追従 0/20）
- **見張り**: Cloud Scheduler が毎朝 Cloud Run ジョブを起動。読み取りスコープだけの同意（書き込みが混ざると起動で落ちる）。床つき状態機械は LLM 不在。Gemini は「なぜそう見えるか」の説明と延長提案だけ（5 筋書き × N=5 で 21/21）
- **承認**: 確認者へは HMAC 署名つきの一問リンク。答えは二択のみ
- **執行**: 決定的ゲート（16/16）。判定はハッシュ鎖で追記専用バケット（保持 400 日）へ。封印はブラウザで KMS 公開鍵により行い、復号は attestation を通った執行イメージの digest ひとつだけに許可（承認外 digest は KMS が 403、実測）
- **最小権限**: Vertex は predict だけのカスタムロール、Scheduler の起動権限はジョブ 1 つ、利用者ごとの秘密と置き場

## 審査向け情報

正直な限界: OAuth は Testing（本番は CASA が要る）、各社の解約手続きの自動化は未実装（解約は依頼メール）、確認者への送信元は Workspace の委任で用意する。

- デプロイ URL: https://atonokoto-web-52kgcfrghq-an.a.run.app/web/index.html
- GitHub: https://github.com/ccie26302/atonokoto （公開、main）。ダッシュボードから GitHub App で連携する。提出後はデフォルトブランチを 12/1 まで凍結

### 動作確認の方法（審査・運営のみ閲覧）

Google ログインは不要です。トップの「審査用アカウントで入る」から次の ID とパスワードで入ってください（secrets/judge.json の値を転記。ここには書かない）。

- ID: judge
- パスワード: （提出時に転記）

入ると、架空の山田太郎の地図（資産 19 件、遺志、確認者 2 人、あとがき、見張りの状態、90 日の足跡）が入った状態で始まります。何を変えても、ログイン画面の「初期化して入る」で元に戻ります。

見どころ:
1. 星図。暗い星が「使っていないのに払っている」。星を押して遺志を塗る、プランを直す、ログイン経路を選ぶ
2. 「一言で」に「写真は妻に」と入れると聞き返す。「サブスクは全部止めて」は提案を出し、本人が「この通りにする」を押すまで塗らない
3. 「架空の人の地図を見る」（ログイン不要）で、「いま走らせる」: 棚卸しエージェントが受信箱 72 通を本当に読み、注入メールを報告する様子がそのまま流れる（2〜3 分）
4. 同じページの「通してみる」: 82 日を数秒に圧縮し、沈黙 → Gemini の判断（予定題名の注入を無視）→ 確認者 → 最後の通知 → 7 日のブレーキ → ゲートの拒否 2 件 → 執行 → 遺族へのご報告（伏せ字つき）
5. 「本物の enclave で開ける」: 本物の Confidential Space を起こし、封印を開ける（約 5 分、30 分に 1 回）。digest と開けた証拠のハッシュが出る

## 提出物のチェック

- [ ] GitHub リポジトリの連携（push 済み。ダッシュボードで GitHub App の連携が残り。デフォルトブランチを提出時点で凍結）
- [ ] デプロイ URL
- [ ] 動作確認の方法（審査用 ID/パスワード。パスワードは secrets/judge.json から転記）
- [ ] プロジェクト概要（300 字）
- [ ] 課題と解決（800 字）
- [ ] アーキテクチャ説明（800 字）
- [x] アーキテクチャ図（docs/architecture.png、2000×1260）
- [ ] 画面キャプチャ（最大 10 枚）
- [ ] デモ動画（YouTube、3 分程度）
- [ ] Zenn 記事（任意、Idea カテゴリ）
- [ ] 最終審査への現地参加承諾（12/1 渋谷）
