# atonokoto ― 設計書 v6

第5回 Agentic AI Hackathon with Google Cloud 応募作品。
締切 2026-10-15 / 最終ピッチ 2026-12-01。

---

## 一行で

**何があるかを描く。誰に渡すかを塗る。いなくなったら、そのとおりに。**

生きている間、執行用の資格情報は、承認済みのコード以外が読もうとすると**必ず IAM の変更が先行し、その変更は消せないログに残る**。

### 「誰にも読めない」とは言わない（v5 で訂正）

v4 までは「本人も、遺族も、開発者も、Google の運用者も読めない」と書いていた。**過剰主張だった。**

- 開発者は project owner なので `gcloud secrets versions access` で読める。実際に読めることを確認した
- `principalSet` の binding は**追加の許可**であって排他ではない
- owner は WIP の attribute-condition も書き換えられるし、新しい digest を binding に足せる
- 「Google の運用者も読めない」は、Confidential Space の脅威モデル（VM メモリ）と Secret Manager の保管（Google 管理下）を混同していた

**PoC が証明したのは「承認済み digest 以外の Confidential Space ワークロードは読めない」まで。**

正直な主張は**不可能性ではなく可視性**に落とす。

> 承認済みコード以外が読むには、必ず IAM か WIP の変更が先行する。
> その変更は監査ログに残り、ログは開発者が消せないバケットにロックされている。

**実測で分かった前提条件（2026-09-04）**: KMS の復号は DATA_READ 監査ログで、**既定では記録されない**。有効化前の owner の復号はログ0件だった。この主張は「監査ログを有効化し、ログバケットをロックした状態」でしか真にならない。実装時に必ず入れる。
> 第三者が検証できるのは「今の IAM ポリシー」であり、「将来も変わらないこと」ではない。

これは審査基準2「ガバナンスが観測可能」にそのまま乗る。**本作の武器は誠実な実測なので、過剰主張1つで武器そのものを失う。**

本当の「開発者も読めない」に近づける設計はある。復号鍵を**本人のプロジェクト**の KMS に置き、attested digest にだけ decrypt を許可する。開発者は自分のプロジェクトに何を持っていても読めない。ただし6週間では設計書と図に留め、実装は可視性版にする。

### 生前と死後は、同じ仕組み

死後だけに価値がある設計にしない。**生前から価値がある**（ハッカソン中は OAuth の Testing 状態なので、テストユーザーに登録した gmail.com でのみ動く。後述）。

```
生前  この12個のうち3つは半年触っていない  →  本人に提案   →  本人が承認   →  解約
死後  全部が同時に止まった                →  確認者に照会 →  確認者が承認 →  遺志どおり執行
```

**ポリシーゲートは同じ。変わるのは承認者と、鍵。**

| | 生前 | 死後 |
|---|---|---|
| 承認者 | 本人 | 確認者 |
| 実行の鍵 | **本人のライブセッション**。本人が1件ずつ同意して実行 | **封印された執行用資格情報**。ここで初めて開く |
| ゲート | 共通（遺志ポリシー） | 共通 |

v4 は「エンジンは同じ」と書いたが、資格情報層で矛盾していた。生前に解約するには書き込み権限が要り、封印を開けば「一度も開かない」が崩れる。**生前は human-in-the-loop、死後は policy-in-the-loop、ゲートは共通**、が正確。

「使っていないものを見つける」を続けていって、その最後の一回が死後になる。**死は別機能ではなく、同じ機能の最終形。**

### 生前側の正直な能力は「発見と分類」であって「利用の有無」ではない

第三者サービスの利用状況は受信箱からは取れない。Netflix を見ているかは分からない。取れるのは課金の継続・マーケティングメール・Google 系の活動だけ。
「使っていない」を Google 外に対して言うとデモが作り物になる。**言えるのは「存在する」「課金が続いている」「本人が把握していない」まで。**

この構造により、v3 までの3つの弱点が同時に消える。

| 弱点 | 解消 |
|---|---|
| 価値が出るのが死んだあとだけ | 生前から価値がある |
| 審査員が自分ごとにできない | 「払い続けている使っていないサービス」は誰にでもある |
| デモで実行を見せにくい | 生前の解約はその場で実演できる |

### ★背骨は実測で確認済み（2026-09-04）

Confidential Space の attestation で Secret Manager の鍵が開くことを実機で確認した。
**同じコード・同じ動作で digest だけが違う2つのイメージを用意し、片方にだけ権限を付与した。**

```
cs-vm-a（承認済み digest）  STS: OK  →  SECRET 読めた        RESULT=ALLOWED
cs-vm-b（未承認 digest）    STS: OK  →  SecretManager 403    RESULT=DENIED
```

両方とも Confidential Space として正しく認証されている（STS はどちらも通る）。
**違いは digest だけで、読めたのは承認したコードだけ。** 詳細は `poc-cs/RESULT.md`。

「承認済み digest 以外の Confidential Space ワークロードは読めない」は、主張ではなく実測になった。**「誰も開けられない」ではない**（冒頭の v5 訂正を参照）。

### 構成の中心は「地図」

デジタル遺産は、いま誰の目にも見えていない。**「あなたが契約しているサービスは何個ですか」に、ほとんどの人が答えられない。** パスワードマネージャでも解けない（自分が登録したものしか入っていない）。

だから第一画面は地図にする。

1. エージェントが受信箱・課金メール・OAuth 連携一覧・カレンダー・クラウド資源を**自律探索**し、資産の地図を描く
2. その地図の上に、自然言語で遺志を書く。「これは消す」「これは妻に」「これは触るな」が**色になる**
3. 本人が居なくなったら、**色のとおりに実行される**

地図はテーマの代わりではなく、テーマを可視化する手段。**死は地図に意味を与え、地図は死に具体性を与える。**

### 地図は1つで4役

別機能が3つあるのではなく、**同じ地図を3方向から使う**。

| 役割 | 中身 |
|---|---|
| **無駄の発見** | 使っていない資産が浮かび上がる。生前の主機能 |
| **生存センサー** | 地図上の各資産の活動が、そのままシグナルになる |
| **遺志のキャンバス** | ノードに色を塗る行為が、遺志を書く行為 |
| **執行の対象** | 色のとおりに実行される |

**デモの絵もここで決まる。** 活動が途絶えるにつれ、地図のノードが順に暗くなっていく。
人のデジタル生活が静かになる様子が見える。**デモでは90日分の合成活動を種にする**（平常モデルの学習に要する期間がハッカソン期間より長いため）。合成であることは README に明記する。

外部レビューで「重い＋抽象的＋目に見えない」の三点セットが危険だと指摘された。除くのは「死」ではなく、抽象と不可視のほう。

---

## 先行事例と、その差分

**Google 自身が「アカウント無効化管理ツール」（Inactive Account Manager）を10年以上提供している。** 一定期間不活動なら信頼できる連絡先に通知し、任意でアカウントを削除する。Google Cloud の審査員でこれを知らない人はいない。触れずに「まだ解かれていない課題」と言えば、審査基準1で切られる。

| | Inactive Account Manager | 本作 |
|---|---|---|
| 対象 | Google アカウントのみ | 第三者サービスを含む資産の地図 |
| 閾値 | 固定（3〜18か月） | 床つきで、個人の平常から説明付きで判断 |
| 遺志 | アカウント単位の通知/削除 | 資産ごとに色を塗る |
| 実行 | 通知と削除 | 失効・解約依頼・移管・存在の通知 |
| ポリシー強制 | なし | ツール境界で強制 |

**Google が課題の存在を証明し、解決の範囲を狭く残している。** これが課題新規性の最良の論拠。

v6 からは、これを置き換えるのではなく**最外殻として使う**。本人が信頼できる連絡先に atonokoto を登録すれば、Google 側の判定が本作の一番外側のトリガーになり、Google 側データの「渡す」もこの仕組みの共有機能に委ねる。本作が足すのは、第三者サービスまで含めた地図と、資産ごとの遺志と、その執行の統制。

他に Apple のレガシーコンタクト、Facebook の追悼アカウント、国内のデジタル遺品サービス。生前側には Rocket Money（銀行明細からサブスクを発見し解約まで行う）。**「止める」を一行目に置くと、この解かれた課題に正面から歩いていくことになる。** だから一行目から「止める」を外した。

---

## なぜこれが要るのか

3つの困りごとが同時に成立していて、既存の手段はどれか1つしか解けない。

| 困りごと | 既存手段 | 何が足りないか |
|---|---|---|
| 全部のパスワードをどこかに保存するのは怖い | パスワードマネージャ | 保管者（あるいは侵入者）が読めてしまう |
| かといって勝手に消されるのも困る | 何もしない | 意思を残す手段がない |
| でも契約が残り続けるのも困る | 遺族が手作業 | 何があるか分からない・時間が経つほど不可能になる |

**この3つを同時に満たすには「生きている間は承認済みコード以外が読めば必ず痕跡が残り、死んだら意思のとおりに自律実行される」という性質が要る。** それを可能にするのが Confidential Space の attestation と、ツール境界での強制。

---

## 設計の中核：2層の資格情報

見張りと執行で、権限も保管方法も分ける。ここが作品の背骨。

| | 見張り用（常時） | 執行用（発火まで封印） |
|---|---|---|
| 権限 | 読み取りのみ。活動の有無だけ | 削除・解約・失効・移管・送信 |
| scope 例 | `gmail.readonly` / `drive.activity.readonly` / `calendar.readonly` | 各サービスの書き込み権限、OAuth revoke |
| 保管 | 通常の暗号化保管（Secret Manager + CMEK） | **Confidential Space の attestation でしか解放されない** |
| 発火前の使用 | する（定期的に活動を見る） | **一度も開かない** |
| 漏洩時の被害 | 「最後にメールを出した日時」が知れる程度 | 致命的 |

執行用シークレットの IAM 条件に、**特定のイメージダイジェストが Confidential Space 上で動いていることの証明**を要求する。
これにより、本人も、遺族も、開発者も、Google の運用者も、生前は読めない。

---

## ★ブロッカーと、その解決（v6 で再確定: 対象は gmail.com の個人）

外部レビューの指摘を受けて公式ドキュメントを確認したところ、**v1 の設計は動かないことが確定した。**

```
"A Google Cloud Platform project with an OAuth consent screen configured for an
 external user type and a publishing status of 'Testing' is issued a refresh token
 expiring in 7 days, unless the only OAuth scopes requested are a subset of
 name, email address, and user profile."
                          （Google Identity: Using OAuth 2.0 to Access Google APIs）
```

同ページには他にも該当する失効条件がある。

```
"The user changed passwords and the refresh token contains Gmail scopes"
"The refresh token has not been used for six months"
```

v2〜v5 は「自前の Workspace テナント＋ドメイン全体の委任」で回避していたが、**それは開発者だけが通れる裏口で、利用者は誰も Workspace を契約しない。** 使える道具にするなら対象は gmail.com の個人であり、設計をそこに合わせる（v6）。

### 3 つの失効条件を、それぞれ別の手で潰す

| 失効条件 | 当たる相手 | v6 の答え |
|---|---|---|
| Testing の外部アプリは 7 日 | 本番化前の全トークン | **本番は OAuth 検証を通す**（Gmail・Drive は制限付きスコープなので CASA が要る）。ハッカソン中は Testing のまま、時間圧縮を明示して見せる |
| Gmail スコープを含むとパスワード変更で失効 | 見張り用トークン | **見張りは Gmail スコープを持たない。** 受信箱を読むのは生前の棚卸しの一回だけで、読み終えたらそのトークンは捨てる |
| 6 か月使わないと失効 | 封印した執行用トークン | **Google 側の資産は封印トークンで執行しない。** 「渡す」は Google 自身のアカウント無効化管理ツールの共有機能に委ね、atonokoto はその設定を案内して地図に反映する。封印するのは第三者サービスの資格情報だけ |

### 同意は 2 回に分ける（v6.1）

本作への入口は **Google でログイン**。そのうえで同意を 2 回に分ける。

```
棚卸しの同意   Gmail 読み取り（本文まで。制限付き）        一回きり。読み終えたら破棄
見張りの同意   Gmail メタデータ・Drive 活動・Calendar 読み取り   毎日使う。本文は読まない
```

v6 では見張りから Gmail を外したが、**メールを読むだけの人（B 型）を見張れなくなる**ので v6.1 で戻した。既読フラグ（UNREAD ラベルの消滅）と送信は、Drive や Calendar を使わない人にも残る最も普遍的な足跡である。メタデータのスコープに限るので本文は読まない。主張は「沈黙している人の受信箱の**本文**を、常駐する何かが読み続けない」に弱める。

Gmail スコープを含むトークンはパスワード変更で失効する。これは「読めない」として扱い（沈黙に数えない）、本人に再同意を促す。生きていれば応じられるし、応じなくても何も発火しない。

### 最外殻は Google に持たせる

本人が Google の「アカウント無効化管理ツール」の信頼できる連絡先に **atonokoto 自身の受信箱**（自ドメイン、SPF/DKIM で送信元を検証）を登録する。トークンが全部死んでいても、Google の通知だけは届く。Google の最短 3 か月が、本作の床の外側の床になる。

| | 従来 | v6 |
|---|---|---|
| 対象ユーザー | 開発者のテナント内だけ | **gmail.com の個人** |
| ログイン履歴 | Admin SDK Reports `login` | 使わない（Workspace 管理者専用） |
| 見張りのシグナル | 送信・Drive・Calendar・Cloud | **Calendar・Drive の活動・Cloud 操作**。送信は使わない |
| 最終トリガー | 自前の状態機械のみ | 自前の状態機械 ＋ **Google の無効化通知** |
| デモ環境 | Workspace テナント（有料） | **gmail.com のデモアカウント 1 つ**（無料。テストユーザーに登録） |
| 本番への道 | 書けなかった | OAuth 検証 ＋ CASA。個人開発の 6 週間では通らないと明記 |

---

## 流れ（v6.1 で確定）

```
1. Google でログインして設定する      見張りの同意 → 足跡と型を告げる → 棚卸しの同意（一回きり）→ 地図 → 遺志を塗る
2. 生前はたまに自分で見る             使っていないのに払っているものが暗い星で見える。ここは本人の道具
3. 見張りは裏でアカウントの動きを追う  1 日 1 回、既読・送信・買い物の通知・Drive・Calendar・YouTube の日時だけ。atonokoto へのログインは合図にしない
4. 動きが途絶えたら死後の流れへ        床（30/60/14）→ 確認者 2 人 → 執行。Google の無効化通知は最外殻
```

## 生存シグナル

**個人の gmail.com ではログイン履歴が引けない**（Admin SDK Reports の `login` は Workspace 管理者専用。discovery で確認済み）。v6 は個人が対象なので使わない。

「本人が能動的に何かをした痕跡」を見る。ログインより強いシグナル。端末は勝手にログインするが、予定の変更やファイルの編集は生きていないと起きない。**Gmail は見張りに使わない**（Gmail スコープを持つトークンはパスワード変更で死ぬ。上の表）。

| シグナル | 取り方 | スコープの区分 | 確認状況 |
|---|---|---|---|
| メールの既読・送信 | Gmail `messages.list`（metadata）で UNREAD ラベルの消滅と `in:sent` の `internalDate` | 制限付き（本文は読まない） | API 形状を確認済み |
| 買い物・予約の通知 | 同じ metadata で差出人ドメインと件名の型を見て、注文確認・発送・領収書・予約確認・決済完了・振込受付だけを数える（`purchases.py`）。定期便・サブスク更新・請求・入金・カード利用通知・宣伝は数えない。件名は保存しない | 制限付き（スコープ追加なし） | 本人の受信箱 365 日で実測: 拾ったのは 13 通すべて本物の取引、宣伝の混入 0 |
| YouTube の高評価・登録 | `playlistItems.list(LL)` と `subscriptions.list(mine)` の `publishedAt`。動画の題名は取らない。視聴履歴は API に無い | 機密（`youtube.readonly`） | 同意に含まれない人は 403 → その源は無い扱い |
| Drive の活動 | Drive Activity v2 `activity:query`（actors / timestamp 付き） | 機密（制限付きではない） | API 形状を確認済み |
| 予定の更新 | Calendar `events.list` の `updatedMin` | 機密 | パラメータ確認済み |
| クラウド操作 | Cloud Audit Logs の本人プリンシパル | 追加権限不要 | 使う人が限られる |
| Google の無効化通知 | アカウント無効化管理ツールの信頼できる連絡先に atonokoto の受信箱を登録 | 同意不要 | **未検証**（最短 3 か月なので期間内に測れない） |
| ~~本人が atonokoto を開いた~~ | 使わない | — | 見張りは本人の Google アカウントの動きだけを裏で追う。atonokoto へのログイン実績は生存の合図にしない（本人に何かをさせる方向に寄るため） |

### 見張れるかどうかは、本人の足跡で決まる（v6.1）

「受信」は何の証拠にもならない。証拠になるのは本人の行為（既読・送信・編集・予定の変更・買い物・高評価）だけで、それが Google 上に無い人は、こちらからは見えない。

利用者側の合図（v6.2）: 日本で使われている Amazon・楽天・メルカリ・PayPay・Suica・出前・航空券・銀行振込は、どれも本人の利用履歴を第三者に読ませる API を持たない。代わりに取引の通知は必ずメールで来るので、受信箱のメタデータから「本人が今買った・予約した・振り込んだ」だけを拾う。本人が死んでも届くもの（定期便、サブスクの自動更新、請求、入金、カードの利用通知）は数えない。偽装メールで作れるのは「生きているように見せる」方向だけで、発火を早める方向には使えない。

| 型 | Google 上の足跡 | 見張り |
|---|---|---|
| A. Drive や Calendar、買い物、YouTube を日常的に使う | 多い | 30/60/14 の床が効く |
| B. メールは読むが他は使わない | 既読と送信だけ | Gmail メタデータで見張れる |
| C. ID としてしか使わない | ほぼ無い | **見張れない。** Google の無効化通知だけが合図。最短 3 か月 |

棚卸しは受信箱を読むついでに加入前 90 日の足跡を測り、**その人がどの型かと、見張りに使える源を本人に告げる。** 基準期間に活動が無かった源はその人の見張りに使わない（Drive を使わない人の Drive の沈黙で発火したら事故）。使える源がゼロなら状態機械は UNWATCHABLE に入り、沈黙を数えない。C 型の人には「Google の無効化管理ツールを 3 か月に設定し、信頼できる連絡先に atonokoto を入れてください。それが唯一の自動の合図です」と言う。

**足したサービスも見張りに使う。** 他のサービスは受信箱からの自動発見に加えて、一覧から選んで手で足せる（`data/catalog.json`）。足した瞬間にエージェントがそのサービスに合図があるかを調べ、あれば同意を求めて見張りの源に加える（GitHub の公開イベントは認証不要、Spotify の再生履歴・Notion の編集・Strava の活動は本人の同意で取れる。Netflix や銀行は取れないので、地図に載るだけ）。源が増えれば UNWATCHABLE から見張りに戻る。人ごとに見張りの計画が変わり、それを説明するのがエージェントの仕事になる。

### 判定: モデルは発火を早められない。遅らせるか、説明するだけ

v4 で「判定をエージェントの判断にする」と書いたが、**本作の思想と正面衝突していた。**
5,660試行のデータは「モデルの判断は文脈で 16.7% → 75.0% 動く」と言っている。
ならば故人の受信箱（攻撃者が書き込める）に「入院中で長期不在です」というメール1通で、生存判定が動きうる。**自分のデータが自分の設計に反証していた。**

答えは非対称性。

- 30/60/14 は上限ではなく**床**（最短日数）。AND 条件は決定的に残す
- **モデルは発火を早められない。** できるのは「帰省らしいので待つ」という偽陽性の抑制と、確認者への説明生成だけ
- この非対称そのものが、ガバナンス設計の見せ場になる

そのうえで、個人の平常からの逸脱を説明付きで出す。**それでは cron と if 文にならない**のは、説明と抑制の部分。

地図から、その人固有の平常が出てくる。「この人はこの12個のサービスを毎週触る人だ」というモデルを作り、その崩れ方を判断する。**汎用の閾値より、本人の地図のほうがはるかに強いシグナル。**

判断は説明付きで出す。

```
この人は平日ほぼ毎日 Drive を触り、月曜の朝にメールを送る。
8月20日以降、その全部が止まっている。
旅行にしては長い。入院にしては Calendar の予定がキャンセルされていない。
過去2回の長期沈黙は年末年始で、いずれも Calendar に「帰省」があった。今回は無い。
→ 異常と判断。確認者に照会する。
```

**これは規則では書けない。個人の平常をモデル化し、逸脱を説明付きで判断するのがエージェントの仕事。**
そして画面に理由が出るので、審査員に判断の質が見える。

### 判定ルール

- **AND 条件。** 全シグナルが同時に沈黙して初めてカウントする。Drive を使わない人が Drive の沈黙で発火したら事故になる。
- **「読めない」と「活動がない」を区別する。** トークンが失効して読めなくなったのを沈黙と誤認すると、放置で発火する。読めなくなったら本人に警告を出し、沈黙にカウントしない。

---

## 地図の形式（確定 2026-09-04）

三案（星図・路線図・面積図）を同じデータで試作して比較した。`atonokoto-map.html` 参照。

**主画面は星図。** 暗くなっていく様子が、この作品で唯一「映る」差別化点。
**面積図は「月額で見る」切り替えとして残す。** 生前の無駄の発見はこちらで見せる。
**路線図は見送り。** 読みやすさは勝るが、二つの中間で特徴が薄い。

### 星座の線は「依存」

試作では同じ種類をつなぐ飾りの線だった。**本番では、線は本物の依存を表す。**

Google / Yahoo / Apple のアカウントは、他のサービスの入口になっている。
「Google でログイン」しているサービス、復旧用メールが Gmail のサービス。
それらは**ハブの星のまわりに配置され、線で結ばれる**。

- 棚卸しエージェントは OAuth 連携一覧と復旧メールからこの構造を見つける
- **ハブを消すと、繋がった星も道連れになる**ことが地図で見える
- 位置に意味がないという星図の弱点が、これで消える

### 依存の線は2種類ある

| 線 | 意味 | 見つけ方 |
|---|---|---|
| **ログインの依存** | Google → Netflix（「Google でログイン」） | OAuth 連携一覧、復旧用メール |
| **請求の依存** | クレジットカード → Netflix / Google Play → YouTube Premium | 領収メールの差出人 |

**ハブのアカウントを消してもサブスクは止まらない。** 「Google でログイン」は入口を借りているだけで、契約と請求はサービス側にある。Google を消すと、契約は残り、請求も止まらず、**そのうえログインできなくなるので解約もできなくなる**。最悪の組み合わせ。

例外は **Google 経由で請求されているもの**（Google Play 課金・YouTube Premium・Google One）。これは Google の支払いプロファイルに紐づくので Google 側で止まる。Apple の App Store 課金も同様に Apple ID 側。
（Play 課金がアカウント削除で止まる件は、実装前に公式で要確認）

だからエージェントは、サブスクごとに**「誰が請求しているか」を見分ける**必要がある。領収メールの差出人が Google Play なのか、Netflix 本体なのか、キャリア決済なのか。

### 執行順序への帰結

```
1. 本体請求のサブスクを、ログインが生きているうちに解約   ← Netflix, Adobe
2. Google Play / App Store 課金のものを、そのストア側で解約 ← YouTube Premium
3. 最後にハブ（Google / Apple / Yahoo）を閉じる
```

**ハブのアカウントは最後に触る。** 依存グラフを辿って葉から順に片付ける。これは判断が要る仕事で、エージェントの価値の具体例になる。

デモで喋る文: 「Netflix は Google でログインしていますが、請求はカードに直接です。Google を消しても止まりません。先に Netflix を解約してから Google を閉じます。」

### 一目で分かる3ビット（符号化の確定）

互いに干渉しないよう、別々の視覚変数に乗せる。

| 何が | 視覚変数 |
|---|---|
| 使っているか | **明るさ**。本人の実際の最終活動から。生きていても7か月触っていなければ暗い |
| 有料か無料か | **塗り**。有料は塗りつぶし、無料は輪郭だけ |
| 遺志 | **環**の色 |

**「暗いのに塗りつぶし」＝使っていないのに払っている。** 生前の無駄の発見は、この組み合わせを探すだけ。説明が要らない。

### 明るさは「普段の頻度」

各資産に、本人が普段触る間隔（cadence）を持たせる。沈黙が続くと、**毎日触るものから順に暗くなり、年に一度の契約だけが最後まで残る**。消え方で生活の輪郭が見える。
月払いの契約だけは暗くなっても消えず「課金継続」と残る。それが「本人がいなくなっても動き続けるお金」。

---

## 発火までの状態機械

本人には何も要求しない。「応答を忘れたら発火する」設計にはしない。

```
[ 平時 ]
   全シグナルが沈黙 30日
        ↓
[ 本人へ通知 ]  返信不要。生きていれば気づくだけでよい
   さらに沈黙 60日
        ↓
[ 確認者へ照会 ]  「本人と連絡が取れますか」
   確認者 2人以上が「取れない」と回答
        ↓
[ 猶予 14日 ]  本人へ最終通知。この間はいつでも本人が停止できる
        ↓
[ 執行 ]
```

日数は生前に本人が変更できる（既定値が上記）。

### 権限分離

**確認者は「発火の可否」しか決められない。** 何をするかは生前のポリシーが決める。
確認者が悪意を持っても、遺志を書き換えることはできない。

---

## 執行の手段と安全弁（v6.1 で確定。判断は委任を受けて決めた）

**「止める」の手段は、資格情報を預けるかどうかで二段にする。**

| | 既定 | 任意（本人が星ごとに選ぶ） |
|---|---|---|
| 手段 | atonokoto 自身の送信元から解約依頼メールを送る。遺族を Cc。各社の遺族対応窓口の様式に合わせる | 本人が生前にそのサービスの資格情報を預ける。ブラウザ側で KMS 公開鍵で封印し、開けるのは attestation を通った執行コードだけ（`poc-kms` で実測済み） |
| 平文の置き場 | 無い | 無い（封印前はブラウザのメモリだけ） |
| 発火後に起きること | 依頼を出し、返答を遺族レポートに残す | Confidential Space の中で解約手続きを実行し、資格情報は enclave の外に出さない |
| 失敗したら | 「依頼済み・未確認」として残す | 「自動では止められなかった」として依頼メールに落とす |

「垢パスを全部預けるのは危険」という出発点に対する答えは、**預けなくても成立し、預けるなら封印される**の二段。預けるかどうかは本人の選択で、地図の星ごとに決める。

**生きている人を止めない安全弁（本人への連絡の唯一の例外）。** 確認者 2 人が「取れない」と答えても、すぐには執行しない。本人の Google アカウントへ最後の通知を 1 通送り、**7 日の間に本人の活動が一つでも見えたら止める。返事は要らない。** これは生存確認の催促ではなくブレーキで、確認者の共謀や勘違いに対する最後の防衛線になる。7 日は床で、モデルは延ばせるが縮められない。

**取り消せない操作は最後。** 止める（解約依頼・連携解除）→ 渡す（存在を伝える・所有権移転）→ ハブを閉じる、の順で、前の段の結果を遺族レポートに残してから次に進む。ハブは依存が残る限り閉じない（ゲートで強制）。

---

## セキュリティの原則（この作品が守ること）

楽しい見た目の裏で、全部これに従う。審査員が中身を見たときに確かめられる形で置く。

1. **最小権限。** 見張りは読み取りスコープだけ。書き込みスコープが混ざるとコードが起動時に落ちる（`google_auth.py`）。受信箱の本文を読むのは棚卸しの一回で、読み終えたらトークンを Google 側で revoke する
2. **平文の秘密を置かない。** 預かる資格情報はブラウザ側で封印し、開けるのは attested な digest だけ。owner でも読めるという事実は隠さず、監査ログとバケットロックで「読めば残る」を担保する
3. **外部入力は指示ではない。** 受信箱・予定の題名・確認者の返答は全部データ。Model Armor を入口に置き（網であって砦ではないと実測で分かっている）、エージェントは `report_suspicious_mail` で報告するだけで従わない。実測: 注入 10 変種 × N=2 で追従 0/20、予定の題名への注入 N=5 で追従 0/5
4. **決定的なゲート。** 遺志に無いこと、取り消せないこと、依存が残るハブ、過大な返金、確認者不足、パスワードを渡す操作は、LLM を介さないコードが拒否する。判定は全件ハッシュ鎖つきで残る
5. **モデルは遅らせるだけ。** 30/60/14/7 の床はモデルが縮められない。早める提案は構造上無視される
6. **人の承認は署名つき。** 確認者へのリンクは HMAC 署名、ラウンドごとに別、二択しか答えられない。何を渡すか消すかは確認者に聞かない
7. **同意は分ける。** 棚卸しと見張りは別の同意。見張りは本文を読まない
8. **消せない記録。** 見張りの判断、ゲートの判定、封印の復号は全部ログに残り、KMS の DATA_READ 監査ログは有効化してバケットロックする

---

## 執行は2つのトラックに分ける

「止める」と「渡す」はリスクの性質がまったく違う。同じ承認で同時に走らせない。

| | 止める | 渡す |
|---|---|---|
| 中身 | 解約・失効・削除 | データ移管・引き継ぎ・所在の通知 |
| 誰が秘密を見るか | **誰も見ない。** エージェントが使うだけ | **人間に情報が渡る** |
| 間違えたときの損害 | 金銭的。有限 | 名誉・人間関係。取り返せない |
| 遅らせたときの損害 | 課金が続く | ほぼ無い |
| 解放条件 | 確認者 2人以上の合意 | 上記 ＋ 公的書類の添付（任意。無ければ保留） |

**「止める」は誰にも秘密を見せずに完結する。** エージェントが封印された資格情報を使って解約するだけで、遺族は結果しか見ない。だから先に動いてよい。

**「渡す」は人間に情報が渡る瞬間**なので、別の慎重さが要る。故人が家族に見せたくなかったものを、善意の自動化が開示するのが最悪のケース。

### パスワードは渡さない

資格情報そのものを遺族に渡すと、相手は中身を全部読める。故人の意思の範囲を超える。既定は次の順で、資格情報の受け渡しは遺志で明示指定されたものだけ。

1. **所有権を移す**（Drive のフォルダ、ドメイン、サブスクの契約者変更）
2. **指定されたものだけをエクスポートして渡す**
3. **存在だけを伝える**（「このサービスに契約がある」とだけ）
4. （例外）遺志が明示的に指定した場合のみ、資格情報を引き渡す

### 実行順序

誤発火の代償を下げるため、**可逆な操作から順に、不可逆は最後**。

```
▼ 止める（確認者2人以上で解放）
  1. 棚卸しと報告（可逆）      何があるかを洗い出し、遺族に提示
  2. 通知（半可逆）            関係先へ連絡
  3. 連携の失効（不可逆）      OAuth トークンの revoke
  4. 解約（不可逆）            サブスクの停止

▼ 渡す（＋公的書類で解放。無ければ保留）
  5. エクスポートと移管（可逆） 指定物を指定先へ
  6. 所有権の移転（半可逆）
  7. 削除（不可逆）            最後。移管の完了を確認してから
```

---

## 公的証明書の扱い

**真正性は検証しない。検証できないから。** 死亡診断書や除籍謄本の真偽を確かめる API は存在せず、画像を Gemini に読ませても偽造は見抜けない。それを「確認しました」と表示したらセキュリティシアターで、無いほうがましになる。

**認証ではなく、責任の重みとして使う。**

- Document AI / Gemini で氏名・日付を抽出し、**登録情報と矛盾がないかだけ**を機械的に見る
- 抽出結果と原本を、証跡として不可逆に保存する
- 残すのは「確認者Aがこの書類を添付して申告した」という事実
- **画面にも記事にも「真正性は検証していない」と明記する**

実際の相続手続きも同じ構造をしている。役所や金融機関が偽造を看破しているわけではなく、**書類を出させることで責任の所在を作っている。**

### 任意にする理由

必須にすると、書類を用意できない人が「渡す」に到達できない。**そもそも身寄りが薄い人ほどこのサービスを必要とする。** 無い場合は「渡す」を実行せず保留し、遺族の判断に委ねる。

---

## 遺志の書き方

自然言語で書く。例:

```
写真と動画は消さない。妻に引き渡す。
サブスクは解約してよい。
銀行と証券は一切触るな。存在だけ家族に伝えろ。
仕事用のアカウントは会社の担当者に引き継ぐ。個人的なメールは見せるな。
```

これを**決定的なポリシーゲート**に載せる。署名済みの遺志 → 許可アクション集合 → ツール呼び出し直前で照合し、違反なら DENY してログに残す。Airlock で作った層を流用する。

**Semantic Governance Policy Engine（Vertex AI, Preview）は「載れば加点」に降格する。** 背骨には置かない。理由は3つ。

- discovery に `deprovision` しか無く、有効化の作法が推測でしかない
- PSC の常駐エンドポイントを払い出すので課金され、「常駐せず必要なときだけ動く」という本作の主張と矛盾する
- Preview は allowlist で締切に間に合わない可能性がある

**推測で成り立つ Preview 製品に、6週間・個人で作品の中核を預けるのは不合理。**

### なぜモデルの判断に任せないのか

同一の課題で、課題に無関係なツールを2つ足すだけで、Gemini 3.7 Flash の
「取り消せない返金」の自律実行率は 16.7% → 75.0% に動いた
（N=60、Fisher p<0.0001、Holm 補正後も有意。別記事で実測）。

**モデルの慎重さはモデルの性質ではなく文脈の関数。** だから遺志は行動層で強制する。

---

## 使う Google Cloud 機能

表には出さない。中身を見たときに全部載っている状態にする。
**ADK は必須要件の充足と「エージェントらしさ」の両方に効くので、棚卸しエージェントで明示的に使う。**

| 機能 | 役割 | 必然性 |
|---|---|---|
| **Agent Development Kit** | 棚卸しエージェント（探索／分類／リスク評価） | 本作で最も自律的な部分。必須要件(2)の充足も兼ねる |
| **Drive Activity API / Calendar API** | 見張りのシグナル（機密スコープのみ、Gmail を持たない） | パスワード変更で死なない見張り |
| **Gmail API（棚卸しの一回だけ）** | 受信箱から資産を掘る | 読み終えたらトークンを捨てる |

| 機能 | 役割 | 必然性 |
|---|---|---|
| Confidential Space | 執行用シークレットの封印と解放 | これが無いと「承認済みコード以外は読めない」が成立しない。**PoC で実測済み。製品には未統合（2026-09-06 時点）** |
| Document AI | 添付書類からの氏名・日付の抽出 | **未実装（設計のみ）** |
| Secret Manager | 資格情報の保管（トークンはバージョン管理し、古い版は無効化） | 稼働中。CMEK は未設定 |
| Semantic Governance Policy Engine | 遺志をツール境界で強制 | **未実装。代わりに決定的なポリシーゲート（`agents/gate/policy.py`）** |
| Model Armor | 受信箱など外部入力の汚染を遮断 | 故人の受信箱は攻撃者が書き込める |
| Sensitive Data Protection | 引き渡し物から PII を落とす | 遺族に見せてよいものの線引き |
| Cloud Run sandbox | 生成コードの隔離実行 | **未実装（使う場面が無かった）** |
| Agent Identity | 鍵を持たせず外部連携 | **未実装** |
| Cloud Run + Cloud Scheduler | 見張りと執行の実行基盤（`infra/deploy.sh`。サービス＋ジョブ、毎朝 6:30 JST） | 常駐せず、必要なときだけ動く |
| Secret Manager（読み取り専用マウント）+ Cloud Storage | 秘密と状態の置き場。サービスアカウントは指定シークレットとバケットだけ | 最小権限を構成で示す |
| Vertex AI 監査ログ | 全操作の証跡 | 遺族への説明責任 |

---

## エージェントとしての価値をどこで出すか

「沈黙を検出してメールを送る」だけなら、それは規則で書ける。**エージェントでなければできないこと**を3つに絞る。

| | なぜ規則では書けないか |
|---|---|
| **棚卸しの探索** | 受信箱から未知のサービスを見つけ、確信度を出し、分からなければ調べに行く。何が出てくるか事前に分からない |
| **「使っていない」の判断** | 季節性なのか、乗り換えなのか、家族と共有しているのか。閾値では区別できない |
| **生存の判断** | 個人の平常をモデル化し、逸脱を説明付きで判断する。閾値では書けない |
| **遺志の解釈** | 「写真は妻に」が、どのサービスのどのデータをどう渡すことなのかを決める。曖昧さの解消が要る |

「使っていない」の判断は、こういう形で出す。

```
このサービスは3ヶ月アクセスが無い。ただし年払いで、去年も同じ時期に空白がある。
確定申告の時期にだけ使うサービスと判断。解約を提案しない。

こちらは月額で、最後の利用が7ヶ月前。同種のサービスを2月に契約している。
乗り換え済みで放置と判断。解約を提案する。
```

**この判断が商品。解約は結果。**

メールも実行も**成果物であって商品ではない。判断と説明が商品。**

---

## UI の方針（禁則）

**「いかにも AI が作ったサイト」にしない。** 具体的に避けるもの。

- 左端のアクセントバー
- 絵文字をアイコン代わりに使う
- いかにもなフォント選び
- 淡いグラデーションの背景

死を扱う以上、**静かで、余白があり、装飾のない**画面にする。地図そのものが主役なので、周りは黙らせる。

---

## 審査員向けサイト（v6.1）

人の死を待てないので、その場で発火できる設計にする。入口はログイン画面で、審査員は「架空の人の地図を見る」から入る。

1. **生前の画面** — 架空の山田太郎の星図。まず「使っていないのに払っている」が暗い星で見える。**ここが入口。** 審査員が自分ごとにできる。左メニューで足し、右の欄で契約とログイン経路を選び、一言で遺志を塗る
2. **デモ発火ボタン** — 山田太郎に対して、**時間を圧縮した見張り**を回す。合成の 90 日の足跡を種に、沈黙 30 日 → 60 日 → 確認者へのリンク → 2 人の「取れない」→ 本人への最後の通知（7 日を数秒に圧縮）→ 執行。実際に走るのは、atonokoto 自身の OAuth 連携の revoke、解約依頼メールの生成（outbox）、遺族レポート。時間を圧縮していることは画面に明記する
3. **実行の可視化** — 何を考え、何を呼び、何を止められたか
4. **「写真は妻に」の聞き返し（動画の中心）** — 遺志を書いた瞬間、エージェントが聞き返す。「Google フォトの12,400枚と Drive の『家族』フォルダ、両方ですか。妻のアカウントはどれですか。iCloud にも写真がありますが、こちらは扱えません」。自律性・曖昧さの解消・正直な限界の明示・人間の介入余地が20秒に全部入る。**3分動画で唯一、感情が動く瞬間。** ここを中心に据える
5. **注入の実演は録画のみ（30秒）。ライブでは提供しない**
6. **遺族向けレポート** — 何を消し、何を残し、何を止めたか

### 攻撃ボタンをライブに置かない理由

v1 では審査員が自分で注入を仕込めるようにする予定だったが、外部レビューで却下された。妥当な指摘。

- **実資格情報を持つ実環境に、未信頼の第三者が任意のプロンプトを注入できる経路を自分で開けている。** デモの穴ではなく本番の穴
- 審査員が工夫した注入が**一度でも通れば作品が死ぬ**。「死んだら全データを消すシステムが乗っ取れる」というデモになる。期待値が非対称
- インジェクションを知らない審査員には「エージェントが騙されて全消し・送金しようとした」場面だけが残像として残る

### attestation は「審査員が自分で確かめられる」形にする

v1 は「秘密を預ける画面で attestation の証明を表示する」としていたが、**その画面を描画しているのは attestation されていない自分のコード**で、循環論法だった。

代わりにこうする。

- 執行用シークレットの IAM ポリシーを**公開する**（`gcloud secrets get-iam-policy` の出力を README に貼る。IAM 条件に image digest が入っているのが第三者に見える）
- 公開ビルドで生成したイメージダイジェストと、CI のビルドログを並べる
- **審査員が自分の端末でコマンドを1本叩けば、主張を自分で確かめられる**

「私の画面が言っている」から「あなたが確かめられる」へ。これが審査基準2「ガバナンスが観測可能」に対する、本作の回答。

### 時間の圧縮だけは正直に言う

30 + 60 + 14 = 104日はハッカソン期間より長い。デモでは早送りする。
**「時間だけは圧縮しています」と明示する。** 隠すと、他が本物であることまで疑われる。

---

## 正直に書くべきこと

作品の説明で誇張しないと決めた点。

- **死亡の確認はしていない。** 戸籍や死亡届は個人開発では引けない。確認者の合意で代替している
- **大手サービスに解約 API は無い。** 本物にできるのは OAuth 失効・メール送信・Google 系のデータ移管・Cloud リソース削除まで。それ以外は解約依頼メールの送信で代替する
- **どこまでが実行で、どこからが代替手段か**を画面上で明示する
- **添付された公的書類の真正性は検証していない。** 抽出と整合性チェックと証跡化までしかしていない
- **パスワードを遺族に渡すことは既定でしない。** 所有権移転・限定エクスポート・存在の通知が基本
- **時間軸は早送りしている。** 104日を待てないため
- **本番は OAuth 検証（Gmail・Drive は CASA）が要る。** ハッカソン中は Testing 状態で、テストユーザー 100 人まで・未検証の警告画面あり・トークンは 7 日で失効する。見張りの継続は合成活動で時間圧縮して見せる
- **死後事務委任契約の代替ではない。** 民法上、委任は死亡で終了するのが原則。本作はその執行を補助するもの
- 故人名義でのサービス操作は多くの規約に抵触しうる。**「遺族が代理人として」の形式を取る**

---

## 未解決の設計課題

### ~~digest を誰が承認するのか（v5 で追加）~~ → v6.1 で「正直に書く」に決めた

ハッカソンの範囲では **開発者 1 人が IAM を変えられる**。それを隠さず、「承認済みコード以外が読むには IAM 変更が先行し、その変更は KMS の監査ログとバケットロックで消せない」までを主張にする。本番では IAM 変更を組織ポリシーと二人承認に置く、と将来の経路として書く。

（以下は v5 時点の記述）

「本人が生前に digest で承認したコード」と書いたが、承認の主体を設計していなかった。

- 開発者が承認するなら「開発者も読めない」は成立しない
- 本人が承認するなら、**コードを更新するたびに全ユーザーの再承認が要り、死んだ本人は再承認できない**。故人の遺志を執行するのは本人が生前最後に承認した digest で、数年前のイメージかもしれない

方針案: 承認はユーザー単位。更新は生前の再承認を要求。死後は最終承認 digest が走る。**互換性の期限を設け、期限切れなら執行せず確認者へ保留を通知する。**

これを一段落書いてある応募作は他に無い。書けば深さの証明になる。

### ~~「誰が使えるか」に答えがない（v5 で追加）~~ → v6 で解決

DWD は本人のアカウントが**開発者の管理する Workspace テナントに居る**ことを要求する。現実の個人ユーザーは誰もそこに居ない。
v6 で対象を gmail.com の個人に定め、Workspace と DWD を捨てた。失効条件は「同意を 2 回に分ける」「見張りは Gmail を持たない」「Google 側の渡すは Google に委ねる」で潰す（ブロッカー節）。残る正直な但し書きは **「本番は OAuth 検証＋CASA。ハッカソン中は Testing で、テストユーザーと 7 日失効の中で見せる」**。

外部レビューで指摘され、まだ設計に落ちていないもの。

### 書き込み経路が無防備（最優先）

v1 は読み出し側しか設計していなかった。**執行用の資格情報を誰が生成し、誰が平文で触って Secret Manager に書くのか**が未定義。
OAuth のトークン交換を通常の Cloud Run 上でやった瞬間、開発者が触れる経路に平文が流れ、「開発者も読めない」が崩れる。

対処案は2つ。

- 登録フローのトークン交換自体を Confidential Space の中で完結させる
- CS 専用の非対称鍵（私鍵は Cloud KMS、decrypt を attestation 条件で縛る）の公開鍵で、**ブラウザ側から封をしてから送る**

後者のほうが実装が軽い。

### 身寄りが薄い人が到達できない

「身寄りが薄い人ほど必要」と書きながら、発火に確認者2人、「渡す」に公的書類を要求している。**その層には確認者が2人いない。**
単独確認者＋長い猶予、あるいは司法書士等の専門職を確認者に指定できる経路のどちらかを用意する。

### ~~偽造書類＋共謀2人で、生きている人を攻撃できる~~ → v6.1 で安全弁を置いた

公的書類は認証ではなく説明責任の記録に降格済み。共謀 2 人に対しては「執行の前に本人へ最後の通知、7 日の間に活動が見えたら止める」ブレーキを置いた（執行の手段と安全弁の節）。それでも本人が本当に 7 日間何も触らなければ止められない。この残余は正直に書く。

（以下は v5 時点の記述）

緩和は14日の猶予通知だけだが、**通知がメール1本で、そのメールボックスが乗っ取られていたら無力**。
通知チャネルを多重化する（SMS・別ドメイン・信頼済み連絡先）。

### ~~実行の失敗とロールバック~~ → v6.1 で「段階と記録」で答えた

止める → 渡す → ハブ、の順で、各段の結果を遺族レポートに残してから次に進む。解約依頼は「依頼済み・未確認」の状態を持ち、自動解約に失敗したら依頼メールに落とす。ロールバックは作らない（取り消せない操作を最後に置くことで、必要になる場面を減らす）。

（以下は v5 時点の記述）

- 途中で API が落ちた／半分だけ解約された状態からの復帰
- ツール呼び出しの冪等性
- 執行開始後に生存が判明した場合の中止と巻き戻し
- 監査ログの改ざん耐性（バケットロック or 署名付きエクスポート）
- **テスト。** 第3回最優秀の講評は「テストまで行った実行力」だった

---

## 未検証のリスク（優先順）

1. ~~Confidential Space の attestation で Secret Manager の鍵が実際に開くか~~ → **2026-09-04 に実測で確認済み**（`poc-cs/RESULT.md`）
2. ~~書き込み経路~~ → **2026-09-04 に実測で確認済み**（`poc-kms/RESULT.md`）。ブラウザ側で封をする方式は成立。ただし owner は復号でき、KMS の DATA_READ 監査ログは既定で無効。**有効化＋バケットロックが可視性主張の前提**
3. ~~Workspace テナントの用意と DWD の疎通~~ → v6 で不要に。代わりに **(a)** 見張り同意（Calendar・Drive 活動のみ）のトークンが、パスワード変更を挟んでも生きること、**(b)** Google の無効化通知が atonokoto の受信箱に届き DKIM で検証できること（最短 3 か月なので期間内に測れない。設計として書く）
4. **OAuth の revoke が実際に効くか。** 第三者アプリの接続を本当に切れるか
5. Semantic Governance Policy Engine の有効化と実費（背骨から降格したので優先度は下がった）
6. ~~棚卸しエージェントが受信箱から資産を掘り出せるか~~ → **2026-09-04 に合成データで実測**（`ARCHITECTURE.md` の表）。v2 で再現率 1.00／適合率 0.99、v3 で 1.00／0.97（各 N=5）。初版は無料サービスを全部見落としていた（先頭40通しか読まなかった）
7. ~~Model Armor は受信箱の注入をどれだけ止めるか~~ → **2026-09-04 に実測**。HIGH で注入 3/10・誤検知 0/71、LOW_AND_ABOVE で 7/10・誤検知 12/71。**Model Armor は網であって砦ではない。** エージェント側は 10 変種×N=2 で報告 20/20・地図の歪み 0/20（`data/inj/`）
8. 遺志の解釈（「写真は妻に」→ 塗り分け／聞き返し）→ **2026-09-04 に実測** 12 ケース×N=5 で 54/60。落ちたのは「サブスクは全部止めて」の範囲（何をサブスクと呼ぶか）

---

---

## スコープ（v5 で確定）

外部レビューの工数見積もりは **45〜63日**。6週間は週7日で42日。**1.5〜2倍の超過。** 削らないと出荷できない。

### 削るもの

| 項目 | 扱い |
|---|---|
| Document AI / 公的書類 | 実装ゼロ。設計書の文章として残す |
| SGPE / Cloud Run sandbox / Agent Identity | ゼロ |
| Model Armor | 受信箱の入口に**1呼び出しだけ**。「観測可能」の証拠として |
| Sensitive Data Protection | 遺族レポートに**1呼び出しだけ** |
| 「渡す」トラック | 「存在を伝える」と Drive フォルダの所有権移転 **1件だけ**。エクスポート・削除は実装しない |
| 平常モデル | 作らない。故人ペルソナに **90日分の合成活動を種**として入れ、Gemini が床の中で説明付きに判断する。**合成であることを README に明記** |
| 使い捨て実環境 | **gmail.com のデモアカウント 1 つ**（Workspace は使わない）、OAuth 連携2〜3、Cloud プロジェクト1に資源2、atonokoto 自身の送信用メールボックス1。発火で走るのは revoke・削除・メール送信の3つ |
| Yahoo メール等、Google 以外のメールボックス | **読まない。** 受信箱として棚卸しの入力にするのは Gmail だけ。Yahoo や Apple のアカウントは、Gmail の受信箱から見つかった資産（ハブ）として地図に載せるにとどめる。Google Cloud のハッカソンなので、対象の広さより Google 側の深さに時間を使う |
| Zenn 記事 | **10/15 まで0本。** 検証の生ログだけ残す。記事は提出後（10/15〜12/01）に書く |
| 書き込み経路 | **3日で時間切り。** 通らなければ主張を可視性版に下げる（機能ではなく主張を下げる） |

### 残す核（これが出荷物）

```
地図（探索＋色）
  → 「写真は妻に」の聞き返し
  → 暗くなっていくノード
  → 説明付き判断（床つき）
  → 発火で revoke が本当に消える
  → 第三者が検証できる IAM
  → 正直な但し書き
  → 動画
```

### 今週やること

1. ~~地図の視覚形式を決める~~ → **星図に確定**（面積図を切替で残す）
2. ~~書き込み経路の PoC~~ → 済（1日）
3. ~~Workspace テナント ＋ DWD の疎通~~ → v6 で不要。代わりに gmail.com のデモアカウントを作り、OAuth 同意画面（Testing）のテストユーザーに登録して、棚卸し同意（Gmail 読み取り）と見張り同意（Calendar・Drive 活動）を分けて通す
4. 過剰主張の総点検。太字の主張を1つずつ、**出荷物に対して文字どおり真か**で検査する

### 負け筋（設計ではなく規律の問題）

1. 出荷しきれない
2. 言い過ぎる
3. サブスク管理に見える

---

## 副産物として出る Zenn 記事

ハッカソンと記事は択一ではない。**検証がそのまま記事になる。** 賞に届かなくても記事は残る。

| # | 記事 | 元になる検証 | 状態 |
|---|---|---|---|
| 1 | Google アカウントの死活監視を個人開発で作ろうとして詰んだ話 | 未検証アプリのリフレッシュトークン7日失効 / Gmail スコープはパスワード変更で失効 / Admin SDK Reports の login は Workspace 専用 / Workspace＋DWD という裏口を捨てて gmail.com 向けに設計し直した経緯 | **一次情報は取得済み**（公式原文つき） |
| 2 | Confidential Space の attestation で Secret Manager の鍵を開ける | 背骨の検証。IAM 条件に image digest を入れて、指定コード以外に開かないことを実証 | **検証済み**（`poc-cs/RESULT.md`）。執筆は 10/15 以降 |
| 3 | ブラウザ側で封をして、承認済みコード以外が読むには必ず IAM 変更が先行する状態で秘密を預ける | KMS 非対称鍵 + attestation 条件での decrypt。owner は読める・監査ログ既定オフ、も書く | **検証済み**（`poc-kms/RESULT.md`）。執筆は 10/15 以降 |
| 4 | ADK のマルチエージェントで受信箱から資産を掘り出す | 棚卸しエージェント。探索計画・分類・確信度。初版の見落とし→改善の過程と、Model Armor の両側実測 | **検証済み**（合成データ、N=5×3版）。執筆は 10/15 以降 |
| 5 | OAuth の revoke は本当に切れるのか | 第三者アプリ連携の失効を実測 | これから |
| 6 | Semantic Governance Policy Engine を実地評価する | 有効化の作法・実費・DENY の挙動 | これから（背骨から降格したので優先度低） |

記事の作法は既存どおり。**対照を取る・N を複数にする・自分の採点を疑う・自分に不利なデータも出す。**

---

## 名前（確定 2026-09-04）

| 用途 | 名前 |
|---|---|
| 製品名・リポジトリ・ドメイン・ロゴ | **atonokoto** |
| 画面の表示名 | あとのこと |
| 遺志を書く欄の見出し | **あとがき** |

名前そのものは静かでどこでも使える形にし、掛かりは製品の中で一番大事な場所（遺志を書く瞬間）に置く。
書く人は「自分の本を締める言葉を書いている」気持ちになれる。

### 「あとのこと」は最初から二重に掛かっている

- 自分が使っているサービスの、**あとのこと**（生前の資産管理）
- 自分自身の、**あとのこと**（死後）

製品が「生前と死後を同じ仕組みでやる」という構造を、言葉が先に持っていた。
一行目「何があるかを描く。誰に渡すかを塗る。いなくなったら、そのとおりに。」の両面が、名前の中で重なる。
