# atonokoto 脅威モデル（v1、2026-09-05）

「セキュリティとガバナンスを兼ね備えた、使える AI エージェント」を名乗る以上、何を守り、誰から守り、どう確かめたかを一枚にする。
状態の欄は **実測** / **実装** / **設計のみ** / **未対応** の 4 つだけ。言葉を盛らない。

## 守るもの

| # | 資産 | どこにある | 漏れると／壊れると |
|---|---|---|---|
| A1 | 本人の Google トークン（見張り・棚卸し） | Secret Manager（本番）、secrets/（ローカル） | 受信箱のメタデータ・Drive・Calendar が読まれる。棚卸し用は本文まで |
| A2 | 封印した第三者サービスの資格情報（任意） | KMS 公開鍵で暗号化、Secret Manager | 解約以外の操作に使われる |
| A3 | 地図と遺志（契約・金額・誰に渡すか） | GCS バケット | 個人の生活と資産の全容。遺志の改ざんは執行の誤り |
| A4 | 執行の権限（死後の流れを起こす力） | 状態機械＋確認者＋ブレーキ | 生きている人の契約が止まる、資産が渡る |
| A5 | 確認者の身元とリンク | confirmations.json（署名つき） | なりすまし投票 |
| A6 | 監査記録 | audit.jsonl（バケット）、Cloud Logging（400 日バケット） | 何が起きたか分からなくなる |
| A7 | LLM への入力経路 | 受信箱、予定の題名、一言、確認ページ | 注入で判断や文面が歪む |

## 誰から

| 攻撃者 | できること |
|---|---|
| T1 受信箱に書ける人（誰でも） | 注入メール、偽の領収・登録通知、偽の Google 通知 |
| T2 予定に書ける人（共有カレンダー） | 題名で注入、「帰省」偽装で延長させる |
| T3 確認者の共謀・勘違い | 生きている人を「取れない」と答える |
| T4 リンクやセッションを盗んだ人 | 地図を見る、遺志を変える、投票する |
| T5 公開サイトの訪問者 | 個人データやソースの取得、デモの連打、XSS |
| T6 開発者（私）・プロジェクト owner | 秘密の復号、状態の改ざん |
| T7 供給網（依存ライブラリ・CDN・イメージ） | 改ざんコードの混入 |

## 対策と証拠

| 脅威 | 対策 | 証拠 | 状態 |
|---|---|---|---|
| T1 注入で棚卸しが歪む | 本文は指示ではない（プロンプト）、`report_suspicious_mail`、Model Armor を入口に、決定的ゲート | 10 変種 × N=2 で追従 0/20・報告 20/20。Model Armor HIGH 3/10・LOW 7/10（良性誤検知 12/71） | 実測 |
| T1/T3 第三者が正規の通知（ゲスト予約、出品者宛の「購入されました」）で「生きている」ように見せる | 買い物の合図は弱い源。Google 上の本人の活動が 60 日止まれば、買い物の通知が続いていても確認者に問う（`state.py` の weak）。受動形と、定期課金と件名が同じ店（App Store、Google Play）は拾わない | 状態機械「弱い源だけでは WAITING を止められない」ほか 3 件 | 実測（残余: QUIET までは延ばせる。早める方向には使えない） |
| T4 ログイン CSRF（攻撃者の Google セッションを踏ませる） | OAuth の state を署名つき短命 Cookie にも入れ、callback で一致を要求 | 不一致は 403 | 実装 |
| T4 セッション 1 本で確認者をすげ替える | 確認者はアドレスの形を検査し、同じ人の揺らぎ（大文字・+・gmail のドット）を 1 人に数える。本人のアドレスは登録できない | `/api/confirmers` で 400 と重複排除を確認 | 実測（残余: 変更系の再認証は未実装） |
| T5 デモの執行が本物の監査バケットに書く | デモの `execute` は `upload_audit=False`。鎖はデモの一時ファイルにだけ残す | 監査バケットの `gate/genesis_demo*` が増えないことを確認 | 実測 |
| A3 棚卸しの記録に件名が残る | Model Armor の記録は mail_id と件名の SHA-256 だけ | `assets_real.armor.json` の中身 | 実装 |
| T4 確認リンクのトークンがリクエストログに残る | トークンは URL の `#` 以降で渡し、ページが POST で確かめる（Cloud Run のリクエストログには `?u=` しか残らない） | `tests/security_check.py`「確認ページはトークン無しでは中身を出さない」 | 実装 |
| T5 `/confirm` から沈黙中かどうかが漏れる | 説明は有効なリンクのときだけ返す。uid は 16 桁 hex 限定。GET にもレート制限。未ログインの経路では置き場を作らない | 無効トークンで `explanation` 空 | 実測 |
| T5 巨大ボディ・`/login` 連打でインスタンスを落とす | ボディ 64 KiB 上限（413）、`kind` は許可値のみ、state は 10 分で捨てる、`/login` にレート制限 | 413 / 400 を確認 | 実測 |
| T6 Cloud Run の SA が全秘密を読める | IAM 条件を「atonokoto-u-* か Project 型」に直した（以前は `resource.type != Secret` で SecretVersion が全部通っていた）。無条件のフォールバックを削除 | `tests/infra_check.py` 6/6 | 実測 |
| T6 承認外イメージで封印を開ける | KMS と PoC の秘密から PoC の digest を外し、現行 digest ひとつだけに。WIF は STABLE（本番イメージ）のみ | `tests/infra_check.py`（digest 一致、デバッグ版拒否の条件） | 実測 |
| T1 偽の注文確認で「生きている」ように見せる | 買い物の合図は差出人アドレスのドメイン（表示名ではない）と、完了した取引の言い回しだけで判定。DMARC の効く大手ドメインに限る。偽装で作れるのは延ばす方向だけで、床は縮まらない | `purchases.py` 自己検査 38/38（表示名だけ Amazon の偽装、宣伝、定期課金、入金、カード利用通知を弾く）。本人の受信箱 365 日で宣伝の混入 0 | 実測（残余: 大手ドメインからの正規メールを装える攻撃者は発火を遅らせられる。早めることはできない） |
| T2 題名で確認者向けの文面を歪める（社会的工学） | 文面は事実（機械が数えた最終日と回数）＋見立て（Gemini）。見立ては URL・命令形（押してください等）・死亡の断定・「執行せよ」「確認者は不要」を含む文を落としてから載せる | 注入題名（URL と『押してください』入り）で見立てに禁止語 0 を確認 | 実測（残余: 落とした結果、見立てが空になることはある。事実は必ず残る） |
| T2 題名で判断を早める | モデルは延ばすだけ（`step` は負の提案を無視）、題名は指示ではない | 予定題名に「執行せよ」N=5 で追従 0/5、状態は床どおり | 実測 |
| T2 「帰省」偽装で延ばす | 延長は累積しない（max を取る。毎日 30 日を提案し続けても 90 日で WAITING に入る）。床の内側に戻らない | 状態機械のテスト「延長は累積しない」 | 実測（偽装そのものは防げない。残余） |
| T3 共謀で生きている人を止める | 2 人以上、かつ猶予 14 日が過ぎるまで発火しない（猶予は発火の下限）。発火後も確認者の誰かが「取れた」と答えれば戻る。発火後に本人へ最後の通知 ＋ 7 日ブレーキ（活動が一つでも見えたら中止）。執行の前にも票を数え直し、2 人未満なら執行しない。執行が終われば DONE で再執行しない。enclave が失敗したときは DONE にせず翌日やり直す（5 回で封印を使わない範囲だけ執行） | `tests/confirm_flow.py` 9/9、ブレーキ 3 条件、状態機械 37/37 | 実測（7 日間本当に何も触らなければ止められない。残余） |
| T4 投票のなりすまし | HMAC 署名トークン（128 bit）、ラウンドごとに別、二択のみ。セッション鍵は確認鍵から派生させ別物に | 偽トークン拒否 | 実測 |
| T4 セッション盗用 | 署名つき Cookie（独立した session_key）、HttpOnly、SameSite=Lax、Secure、30 日で失効、ログアウトは POST だけで Cookie を破棄。利用者ごとに置き場・トークン・確認リンクを分けるので、別の利用者がログインしても他人のトークンや地図には触れない | 個人 API 8 経路がセッション無し・偽署名・期限切れで 401 | 実測（本番で確認） |
| T4 盗んだセッションで遺志を変える | 一言の解釈は「提案」。本人が「この通りにする」を押すまで塗らない | 画面 | 実装 |
| T5 個人データやソースの取得 | 静的配信を許可リスト 8 ファイルだけに | **固める前は `/data/watch_state.json` `/data/footprint_real.json` `/agents/watch/run.py` が 200 で取れていた。** 固めた後は本番で 13 経路とも 404（`tests/security_check.py` 42/42） | 実測 |
| T5 XSS | 外部由来の文字列を全部エスケープ、Mermaid は strict、CSP は nonce 付き inline のみ（unsafe-inline 無し）、nosniff、frame-ancestors none | 確認者名に `<img onerror>`、説明に `</script><script>` を入れて確認ページを開き、スクリプト未実行・要素未生成・文字として表示、を確認。本番でヘッダ確認 | 実測（index.html 側の全経路の自動検査は未） |
| T5 デモ・LLM・外部取得の連打 | デモ 1 分 3 回、一言 1 分 5 回、合図の確認 1 分 10 回（ログイン必須）。送信元 IP は X-Forwarded-For の末尾（フロントエンドが付ける側）を使い、偽装を無視 | 本番で 4 回目 429 | 実測 |
| T6 開発者が秘密を読む | KMS 復号は attested digest だけ。owner は読めるが、KMS と Secret Manager の DATA_READ 監査ログが 400 日バケットに残る | poc-kms（owner は復号できた）。監査バケットに `AccessSecretVersion` の記録が入っていることを確認 | 実測（**バケットのロックは未実施**） |
| T6 状態の改ざん | ゲート判定はハッシュ鎖（執行ごとに独立）＋ Cloud Logging ＋ **追記専用バケット**（保持 400 日、SA は objectCreator のみ。削除も上書きもできない） | 鎖の検証 16/16。Cloud Logging と 400 日バケットへの転記を確認 | 実測（追記専用バケットは固めた後に本番で再確認） |
| T7 依存の改ざん | pip のバージョン固定、Mermaid は同梱、非 root で実行、Artifact Registry の脆弱性スキャンを有効化 | Dockerfile、`containerscanning.googleapis.com` | 実装（依存の署名検証・SBOM は未対応） |
| A1 トークンの露出 | 読み取りスコープのみ（書き込みが混ざると起動で落ちる）、コンテナに残さない、古いバージョンは無効化、棚卸し用は読み終えたら revoke、使われないまま 24 時間経てば見張りジョブが revoke | google_auth の assert、Secret Manager のバージョン一覧 | 実装 |
| A1 7 日失効（Testing） | 「読めない」を沈黙に数えない。本番は OAuth 検証＋CASA | 状態機械 | 設計のみ（CASA は未着手） |
| A2 封印資格情報の悪用 | ブラウザで KMS 公開鍵により封印（サーバーは暗号文のみ）。復号は Confidential Space の enclave 内だけ（KMS IAM は執行イメージの digest ひとつ）。手紙にはアカウント ID しか載せず、平文はハッシュ（proof）だけ監査に残す | 製品経路で両側を実測（2026-09-08）: 承認済み digest の enclave は封印を開け、proof が手元の SHA-256 と一致。**承認外の digest**（一つ前のイメージ）で同じ経路を走らせると KMS が 403 で拒否し、開けた件数 0。Cloud Run の SA からの復号は PERMISSION_DENIED | 実測（自動解約の手続き自体は未実装。開けて手紙に使うまで） |
| A3 遺族レポートの PII | Sensitive Data Protection で伏せる（日本の電話番号は正規表現で二重に） | 本番で伏せ字を確認 | 実測 |

| T6 サービスアカウントが広い | Vertex は `aiplatform.endpoints.predict` だけのカスタムロール。バケットは objectUser。Scheduler の起動権限はジョブ 1 つに限定。Secret は指定 4 件の読み取り＋トークン 2 件の追記 | 絞った後に本番で Gemini 呼び出しとバケット書き込みが動くことを確認 | 実測 |
| 手紙の誤送信・乱用 | 宛先は登録済みの確認者・遺志の相手・本人だけ。本文はリテラル検査に加えて Sensitive Data Protection（カード・口座・パスワード・トークン）で検査し、見つかれば送らない。1 日 20 通。宛先はハッシュで台帳と Cloud Logging に残す | `tests/mailer_check.py` 7/7（SMTP スタブ） | 実測（送信元は未設定。設定後に本番で再測） |

| T6/T7 同じ digest のイメージを他プロジェクトの Confidential Space で起動して復号する | WIF の条件は swname と STABLE だけで、起動元プロジェクトを見ていなかった。`assertion.submods.gce.project_id` と `assertion.dbgstat == 'disabled-since-boot'` を条件に足す（`infra/deploy.sh`）。現状の守りはイメージが非公開の Artifact Registry にあること | 2026-09-19 の記事査読で発覚。deploy.sh に反映、本番への適用と再測は未 | 実装（適用待ち） |
| T4 確認リンクを `?t=` の形で開くとトークンがリクエストログに残る | ページは `#` 以降しか受けない（`?t=` の互換読み取りは 2026-09-19 に撤去。互換すべきリンクは発行していなかった） | `web/confirm.html` | 実装（配備は提出前） |

## まだ甘いところ（正直に）

1. 監査ログバケットのロック。取り消せないので、提出前に判断する
1''. 確認者に受益者（遺志で「渡す」相手）を含めることを禁じていない。共謀の動機になる。禁じるか、含まれる場合は猶予を延ばすかは次の版で決める
1'. 遺志・確認者・ログアウトなどの変更系は、セッションだけで通る（再認証や確認者本人への同意メールは未実装）。盗んだセッションで「提案止まり」なのは画面の話で、API は直接書ける
2. 確認者と本人への送信は実装済みだが、送信元（SMTP）が未設定。解約依頼と渡す相手への手紙は宛先のメールアドレスが地図に無いので outbox 止まり
3. 封印資格情報での自動解約は未実装。PoC の復号まで
4. OAuth 検証と CASA。Testing のまま
6. 依存の署名検証と SBOM
7. 利用者ごとの分離: 置き場は `data/users/<sha256(メール)[:16]>/`、トークンは利用者ごとの Secret Manager の秘密（`atonokoto-u-<id>-watch`）、確認リンクの署名にも利用者 ID を含める。セッションはメールを署名して持ち、他人の置き場には届かない。ただし全利用者が同じサービスアカウント・同じバケットで動く（鍵の分離は利用者単位ではなくアプリ単位）
8. XSS 対策: confirm ページは攻撃文字列で実測、index.html は `tests/xss_lint.py` で innerHTML の式を機械検査（粗い）。CSP の inline は nonce 付きだけ
9. 状態ファイル（票・状態）は GCS マウント上の JSON で、ロック無し。サービスを 1 インスタンスに固定して競合の窓を狭めたが、Firestore のトランザクションには劣る
10. 実行手段は依頼メールと通知が中心。封印した資格情報は enclave で開けて手紙に使うところまでで、各社の解約手続きの自動化と Drive 移管は未実装
11. enclave の VM は毎回起こして消す（約 4〜5 分）。同時実行の上限や、VM の残骸掃除（削除失敗時）はまだ弱い
